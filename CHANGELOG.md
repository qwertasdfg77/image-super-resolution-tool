# 更新记录

## v1.0.4 - 2026-06-07

含默认模型版。
- 包内加入默认 Transformer 模型 `003_ATD_SRx4_finetune.pth`。
- 默认超分不再需要首次联网下载模型。
- 说明文件改为含模型版口径。
- Python 运行环境仍由用户通过软件内 `安装/检查环境` 自行安装。
- 不保存输入图片路径和输出文件夹路径。
- 打开软件时不自动恢复上次图片路径。
- 删除软件内处理前后预览栏。
- 软件安装环境使用 `.bat` 和 Python 脚本。
- 环境安装完成后，当前界面也会优先使用新建的 `.venv` 环境运行超分任务。
