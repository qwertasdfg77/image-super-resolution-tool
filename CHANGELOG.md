# 更新记录

## v1.0.3 - 2026-06-07

轻量包、隐私和体验修正版。
- 不再保存输入图片路径和输出文件夹路径。
- 打开软件时不再自动恢复上次图片路径。
- 删除软件内处理前后预览栏。
- 轻量包不包含 `.venv` 环境和模型权重。
- 软件安装环境改为 `.bat` 和 Python 脚本。
- 环境安装完成后，当前界面也会优先使用新建的 `.venv` 环境运行超分任务。
