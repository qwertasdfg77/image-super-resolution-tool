name: Python check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Syntax check
        run: |
          python - <<'PY'
          import ast
          from pathlib import Path
          for file_name in ["dlss_like_super_resolution.py", "dlss_like_super_resolution_gui.py", "install_gpu.py"]:
              ast.parse(Path(file_name).read_text(encoding="utf-8"), filename=file_name)
          PY
      - name: Lightweight file check
        run: |
          python - <<'PY'
          from pathlib import Path
          bad = []
          for path in Path('.').rglob('*'):
              if not path.is_file():
                  continue
              parts = set(path.parts)
              if '.git' in parts:
                  continue
              suffix = path.suffix.lower()
              if '.venv' in parts or '__pycache__' in parts or suffix in {'.pyc', '.pyo'}:
                  bad.append(str(path))
              if path.parts[:1] == ('models',) and suffix in {'.pth', '.pt', '.onnx'}:
                  bad.append(str(path))
          if bad:
              raise SystemExit('Unexpected generated files:\n' + '\n'.join(bad))
          PY
