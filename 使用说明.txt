图片超分辨率工具含模型版

使用方法：
1. 双击“打开超分工具.bat”。
2. 第一次使用先点软件左下角的“安装/检查环境”。
3. 等环境安装完成后，选择图片和输出文件夹，然后开始超分。

说明：
- 这个包已经包含默认 Transformer 超分模型。
- 默认使用时不需要再单独下载模型。
- 这个包不包含 Python 运行环境和示例照片。
- 环境会由用户电脑通过软件自行下载。
- 如果手动选择照片/动漫备用模型，对应备用模型仍会按需下载。

推荐配置：
- Windows 10 / Windows 11
- NVIDIA RTX 4060 8GB 或更高
